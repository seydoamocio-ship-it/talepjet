create table if not exists demands (
  id uuid default gen_random_uuid() primary key,
  created_at timestamp with time zone default now(),

  main_category text,   -- emlak / arac
  sub_category text,

  title text,
  description text,

  city text,
  price text,

  username text,
  phone text
);